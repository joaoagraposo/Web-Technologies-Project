// ai.js
function aiMove() {
  showMessage("IA a jogar...");
  setTimeout(() => {
    // TODO: Selecionar peça e mover de forma aleatória válida
    showMessage("IA fez a jogada.");
    nextTurn();
  }, 1000);
}
